# minishell — Day 3

## Goal
Add support for background/foreground process management:
- background execution using `&`
- `jobs` to list background processes
- `fg %jobid` to bring a background job to foreground
- `bg %jobid` to resume a stopped job in background

## Build

